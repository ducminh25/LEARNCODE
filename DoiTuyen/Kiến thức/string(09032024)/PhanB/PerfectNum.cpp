#include <bits/stdc++.h>
using namespace std;
int main ()
{
    long long n;
    cin >> n;
    //list of n's divisor in array
    long long div[n+1];
    for (long long i = 1; i <= n; i++)
    {
        div[i] = 0;

    }
}